typedef struct _Abonent
{
  DWORD id;
  LPTSTR name;
  LPTSTR phone_no;
  LPTSTR address;
} Abonent;

BOOL init();
// initialize DB

void finalize();
// end DB session

DWORD find_by_{name,phone_no,address}(DWORD *ids, DWORD max_num, LPCTSTR $1);
// Returns number of matches

BOOL get_by_id(DWORD id, Abonent *abonent);
// Returs 0 if not found, non-zero otherwise

BOOL update_abonent(Abonent *abonent);
// Returns 0 if failed, non-zero otherwise

DWORD insert_abonent(Abonent *new_abonent);
// Returns new id

BOOL remove_abonent(DWORD id);
// Returns 0 if failed
